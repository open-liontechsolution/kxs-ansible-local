apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jacket-ingress
  namespace: plex
  annotations:
    traefik.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: jacket.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: jacket-service
            port:
              number: 9117